# 前回チェック状態

最終チェック日: 2026-08-09

## Claude Code / Claude Developer Platform

- **Claude Code は v2.1.226（8/8）が最新**。`code.claude.com/docs/en/changelog` と `raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md` の2ソースで最上位が一致（B-024 の手順で確定）。v2.1.226 は「バグ修正と信頼性の改善」のみ
- **v2.1.225（8/8）の主要点（本日ハイライト2）**:
  - 一過性の401で長期の `CLAUDE_CODE_OAUTH_TOKEN` が保存済みログインの短命トークンに置き換わり、再起動まで headless セッションが壊れる不具合を修正
  - macOS の MCP OAuth サーバーがキーチェーン読み取りタイムアウト後に401を連発する不具合を修正
  - auto mode が自身の権限チェックへの安全フィルタ拒否を連続ブロック数に数えていた不具合を修正（拒否は維持・モデルには先へ進むよう伝える）
  - セッション間メッセージが headless / 起動中に通知も期限もなく滞留する不具合、巨大会話 compact 後の Remote Control 再開で履歴が壊れる不具合を修正
  - `claude self-hosted-runner` が `--base-dir` を作成・書込できないとき登録だけして全セッションを失敗させる → 起動時に明示エラーで終了
  - Claude Code on the web セッションの「停止」誤報告と再接続ごとのイベント再送を修正
  - ゲートウェイの支出上限が使用量警告に反映（上限額・リセット時刻・運用者メッセージ。ゲートウェイ側も 2.1.225 が要る）
  - `claude agents` に未信頼ディレクトリのワークスペース信頼プロンプトを追加
  - `SendMessage` が他マシンの Remote Control セッションを名前で指定して会話開始可（`ListAgents` に `name [ref]`）。Remote Control の写真添付は直接モデルへ渡る
  - \[VSCode] Focus view が最新 TODO・保留質問の文脈・確定回答を畳む不具合を修正
- **Claude API release notes は 8/7 が最上位のまま**（Managed Agents 4件: セッション予算 / advisor / `inference_geo` / GitHub リポジトリからの skill 読み込み）。8/8・8/9 の追加なし
- 既報の破壊的変更2件（7/24）: Opus 5 で `thinking:{"type":"disabled"}` × effort `xhigh`/`max` は 400 / Opus 4.7 の fast mode 廃止

## Claude 製品 / Anthropic

- **auto mode が 8/14 に Claude Code の既定権限モードになる（8/7 告知・本日ハイライト1）** — `claude.com/blog/auto-mode-default-in-claude-code`。⚠️ **08-07・08-08 の両セッションが検出できておらず 08-09 に2日遅れで初検出**（B-017 に2件目の実例として追記）
  - 対象は Pro / Max / Team。自分で既定を設定済みなら一度だけ切替確認、組織管理の既定は不変。Shift+Tab（CLI）/ モードドロップダウン（デスクトップ）で随時切替
  - 根拠数値: 1,053人の対照試験で危険コマンド検出が人手 13.6% 対 auto mode 89%（人間が承認し auto が捕捉 800件 / 逆は6件）／ 実運用 5〜6月で本番相当の有害な意図せぬ操作を含むセッションが 6.3%（都度承認）対 2.4%（auto）／ Apollo Research の敵対的テストで見逃し率 12%→7% ／ Trajectory Labs の720回プロンプトインジェクションで成功率 0%（競合の自動レビューは 5.83%）／ 実運用で PR 約25%増
  - 管理: managed settings の `defaultMode`（組織既定）と `disableAutoMode`（全面禁止）。分類器設定は `autoMode` の `environment` / `allow` / `soft_deny` / `hard_deny`（自然文）
  - ⚠️ `"$defaults"` を配列に入れないと組み込みルールが丸ごと消える。分類器は `.claude/settings.json` / `.claude/settings.local.json` の `autoMode` を読まない（v2.1.207 以降）
  - v2.1.211 で作業中リポジトリの任意ブランチへの push と PR 作成が既定許可に（それ以前は作業ブランチ等のみ）。`production` / `release` / `gh-pages` は個別判断、force push・秘密混入は引き続きブロック
  - `autoMode.classifyAllShell`（v2.1.193+）で全シェルコマンドを分類器へ。`claude auto-mode defaults / config / critique / reset`（reset は v2.1.212+）
  - Enterprise・Claude API・AWS・Bedrock・Google Cloud Agent Platform・Microsoft Foundry は当面オプトイン。**1ヶ月以内に既定化予定**。分類器のトークン増分は Pro / Max / Team に課金しない
- `support.claude.com` Release Notes は **8/6 の skill / plugin セキュリティスキャン beta が最上位のまま**（8/7〜8/9 の追加なし）
- **Claude 利用枠**: 週次上限50%増は 8/19 まで。Sonnet 5 促進価格 $2/$10 は 8/31 終了（→ $3/$15）
- `www.anthropic.com` はオリジン403継続。WebSearch 5本を実行し 8/8・8/9 の新規企業発表・インシデント公表・助成プログラムは検出せず。AI for Science 希少疾患グラント（Claude API クレジット最大 $50,000・6ヶ月）は **8/2 に応募締切済み**
- 既報: self-hosted environments の public beta（8/6）、Millennium × Anthropic のデジタルリスクアナリスト（8/6）、Inference hooks の Enterprise beta（8/5）、Claude Opus 4.1 退役完了（8/5）
- ⚠️ **`claude.com/blog` は未登録ソースのまま**。8/6 と 8/7 で計2件の取りこぼしが出ている（B-017・回数7）

## 企業構造 / GTM

- 既報で変化なし: Anthropic × Volta の6年 $10B コンピュート契約（8/4）、Tino Cuéllar が初代 Chief Global Affairs Officer に就任（8/4）、SpaceX による Anysphere 買収（6/16 合意・$60B 全株式・Q3 クローズ予定）

## 規制 / 政府

- 8/9 の新規なし。既報: 英 AISI のインシデント報告（8/4 公表）、Anthropic の cybersecurity evals インシデント（7/30 公表・141,006 ラン精査・METR による第三者レビュー中）、ホワイトハウスの任意フロンティア安全性試験枠組み（8/4）
- ⚠️ `www.aisi.gov.uk` はゲートウェイ拒否継続で一次未到達

## GitHub Copilot / Microsoft

- ⚠️ **8/7 付けの github.blog エントリは全部で5件あった**。08-08 のセッションは2件しか取れておらず、本日3件を追加検出した（B-024 に追記）。以下3件が本日分:
- **Copilot code review の effort level が GA（8/7・本日ハイライト3）** — Lite（定型・単純な変更）/ Balanced（複雑・機微・大規模）。public preview の Low / Medium から改名し既存設定は自動移行。組織既定は organization settings → Copilot → Copilot code review、個別設定のないリポジトリは継承。依頼時の選択はそのレビュー1回のみ有効。実行した effort level はタイムラインイベントと PR 概要コメントに表示。対象は Pro / Pro+ / Max / Business / Enterprise
- **Copilot impact dashboard に「Potential return on investment」節が追加（8/7）** — passive / Phase 1 層と Phase 2 / Phase 3 層の2カード。各カードは開発者1人あたり月額コスト（AI クレジット消費ベース）・人件費比率・月間平均 PR 数の3指標。給与セレクタで再計算。⚠️ 試算値であり確定値ではない。導入コホート人数が28日期間中に一度でも活動した全ユーザーを数えるよう修正（ダッシュボードのみ・API と NDJSON は不変）。閲覧権限は Enterprise owner / billing manager / organization owner か「View Copilot Metrics」
- **週次リリースまとめ（8/3 週分）が 8/7 公開** — Copilot App: 処理モデルの表示（クレジット・キャッシュ指標つき）・共有セッションへの直行・`/side` で並行検討・Impeccable スキルのモバイル UI 対応。Copilot CLI: サイドバーで複数セッション管理（`n`/`x`/矢印）・実験的 `/worktree`・Git なしでも動く `/rewind`・ツール呼び出し所要時間の表示。VS Code 1.132: ブラウザ統合の要素単位フィードバック・端末内モデルの多言語ディクテーション・`/btw` の脇質問・Markdown 差分のガター表示
- **Copilot CLI は pre-release 1.0.79-9（8/7 23:39）が最新、安定版 1.0.78（8/3）据え置き**。実質的な変更は 1.0.79-8（8/7 21:29）に集中:
  - Enterprise の allow-auto-only ポリシー対応（allow-all 全体は禁じたまま `/allow-all auto` のみ許可）、Enterprise 管理サンドボックスポリシーによるプロキシ URL 強制
  - 設定キー移動: `sandbox.gitAuth` / `sandbox.ghAuth` → **`sandbox.auth.git` / `sandbox.auth.gh`**（ダイアログに Auth タブ新設）
  - `worktreeBaseRef` 追加（`/worktree` の起点を HEAD かリモート既定ブランチか。既定 HEAD）
  - 大規模モノレポの検索が ripgrep → tgrep。モデルピッカーに Recent / Recommended / New（Shift+Tab で切替）
  - `.venv/bin` / `node_modules/.bin` 等がサンドボックス下で読取専用になる不具合を修正
  - 1.0.79-9 は `/sandbox` 設定ダイアログに保存先を表示する改善のみ
- `github.blog/changelog` は 8/7 の5件が最上位で 8/8・8/9 の新規なし
- 既報: 利用状況メトリクス API のエージェントアプリ活動（8/7）、Code Quality が Copilot を自動レビュアー登録しない変更（8/7）、Kimi K3 の Copilot 追加（8/6）、GitHub Spark 退役（**8/31**）、既定モデル有効化ポリシー発効（**8/26**）、モデル廃止（**9/1**）

## OpenAI / Codex / ChatGPT

- **Codex CLI は安定版 0.147.0（8/7 01:41 UTC）据え置き、pre-release は 0.148.0-alpha.5（8/8 02:26 UTC）が最新**。8/8 に alpha.4（00:43）と alpha.5（02:26）の2本。0.148.0 安定版は未リリース
- `developers.openai.com/changelog` は **8/5 の Fast mode long-context 対応が最上位のまま**（8/6〜8/9 の追加なし）
- `community.openai.com` Announcements RSS は **7/30 の GPT-5.6 値下げ告知が最新のまま**（10日間動きなし）
- ChatGPT デスクトップアプリが内蔵ブラウザのアドレスバーから履歴再訪と Google 検索に対応。コンポーザーに1万字超を貼ると自動で添付ファイル化。`learn.chatgpt.com` はゲートウェイ拒否継続で `site:` 付き WebSearch 経由
- ⚠️ **Codex Remote の全プラン GA は 6/25 の発表**で 8 月の新規ではない（8/8 の検索で混入したため明記）
- 既報: ChatGPT の Free / Go 無制限化と Think ボタン（8/6）、GPT-5.6 Luna 80%減・Terra 20%減（7/30）、Fast mode が Priority Processing を置換、GPT-5.4 / 5.4 mini が 8/31 に Codex（ChatGPT サインイン）から除外、公式 DALL·E GPT が 8/30 退役、Atlas は 8/9 シャットダウン、初号ハードウェアの報道（8/6〜8/7・OpenAI 未公表・2027年見込み）
- **到達性**: `community.openai.com`（RSS）と `developers.openai.com/changelog` は 200。`openai.com` / `help.openai.com` / `platform.openai.com` はオリジン403継続。`learn.chatgpt.com` はゲートウェイ拒否継続

## Cursor / Devin / xAI

- **Cursor changelog は 8/3 の Google Workspace Plugins が最新のまま**。Announcements フォーラムも **7/28 の Cursor Start** のまま。RSS 2本とも取得成功
- **Grok 4.6 は一次確認が依然できない。** 二次サイトは「8/7 ローンチ・1.5T パラメータ・V9 基盤据え置きで SFT / RL に投資」と完了形で書くが、同じ記事群が「アクセスは X / SuperGrok 購読者から順次」「開発者 API と独立ベンチマークは数日遅れ」と未確定形でも書く。2.1T 新基盤の Grok 4.7 が8月末〜9月初との報道も同様に未確認。`x.ai` / `docs.x.ai` / `openrouter.ai` がいずれもゲートウェイ拒否で裏取り経路なし
- **Devin は 8/5 の更新が最新のまま**で 8/6 以降の追加なし。`docs.devin.ai` はゲートウェイ拒否継続で WebSearch 経由

## Google

- **Gemini in Google Docs で本文の隣に画像・図表・インフォグラフィックを作成・編集できる**（生成時に文書の文脈を参照）。⚠️ `workspaceupdates.googleblog.com` がゲートウェイ拒否のため公開日を一次で確定できていない
- **Google Meet の議事メモが画面共有開始時に警告を出す**（Gemini が発表内容のスクリーンショットをメモへ取り込む可能性の通知）
- **Gemini API changelog は 7/30 が最新のまま**で8月の追加なし
- **Gemini 3.5 Pro GA は未ローンチ継続**。8/12 のリーク報道はあるが Google 未発表。I/O（5/19）発表後、6月 → 7月 → 7/17 と3回スリップ
- 既報: Gemini in Classroom の全年齢開放は web **8/10**・モバイル **8/17**、Made by Google **8/12**、Workspace Studio の Gemini Notebooks 自動ソース追加（8/6 開始）
- `blog.google` / `gemini.google` / `docs.cloud.google.com` / `deepmind.google` / `workspaceupdates.googleblog.com` / `support.google.com` はゲートウェイ拒否継続。**`ai.google.dev` のみ到達可**

## MCP（Model Context Protocol）

- **ブログ新着なし**。RSS 最新は 7/28 の `The 2026-07-28 Specification` のままで**12日連続で動きなし**
- 実装側: Codex CLI 0.147.0 が `2026-07-28` をオプトインで実装（MCP SDK 3.0.0）。8/8 の alpha 2本に MCP 関連の追加記載なし
- 仕様 `2026-07-28` — ステートレス化（`initialize`/`initialized` ハンドシェイクと `Mcp-Session-Id` を廃止）、Multi Round-Trip Requests、`Mcp-Method`/`Mcp-Name` ヘッダによるルーティング、`ttlMs`/`cacheScope` による一覧キャッシュ、RFC 9207 issuer 検証必須化と DCR → CIMD 移行
- Tier 1 SDK（変化なし）: TypeScript `@modelcontextprotocol/server` / `client` ともに **2.0.0** ／ Python `mcp` **2.0.0** ／ C# **v2.0** ／ Go は **v2 未発行**で `go-sdk` **v1.7.0** が仕様対応
- `blog.modelcontextprotocol.io`（RSS `index.xml`）は 200。`modelcontextprotocol.io` 本体はゲートウェイ拒否

## オープンウェイト / ローカル LLM

- **8/8〜8/9 に作成された注目のオープンウェイトモデルはない**。HF の作成日降順100件を走査したが、該当期間で likes 5 超または DL 100 超は0件（最大でも likes 1 / DL 0）
- モデル追跡サイトでもフロンティア級の最新は 8/5 の Meta Muse Spark 1.2 で、8/8・8/9 の新規公開なし
- trending 上位（変化なし）: `MiniMaxAI/MiniMax-H3`（7/28 作成）、`deepseek-ai/DeepSeek-V4-Flash-0731`、`moonshotai/Kimi-K3`
- `huggingface.co` は API・HTML とも到達可。`www.deepseek.com` / `api-docs.deepseek.com` / `thinkingmachines.ai` はゲートウェイ拒否継続

## Apple / クラウド

- `developer.apple.com` は 200。最新は **8/5「Get ready for new creative assets on the App Store」**のままで **AI 関連の新規はない**
- iOS 27 / iPadOS 27 developer beta 4（7/20・ビルド 23G71）が最新。GA は9月（予想 9/14 前後）
- `aws.amazon.com/about-aws/whats-new/2026/08/` は 404。月別インデックスのパスが変わっている可能性があり、次回は `about-aws/whats-new/recent/` かリリース個別 URL で試すこと

## 週次復旧チェック

- **直近実施: 2026-08-03（月曜）**。次回: **08-10（月曜・次セッション）**。本日は日曜のため定例チェックは未実施
- **08-09 の判定**: `www.testingcatalog.com` と `simonwillison.net` を WebFetch し、2件とも `EGRESS_BLOCKED` 形式で未復旧。**B-027 の条件に該当するため `curl` での切り分けは省略した**（初適用）
- **復旧8ホストは 08-09 も本文取得に成功**（`claude.com/blog`・`support.claude.com`・`community.openai.com`・`github.blog`・`cursor.com`・`forum.cursor.com`）。復旧は6日連続で定着
- **未解決の要 kit 対応（08-07 に確定・継続）**: 08-06 に追加された許可ドメイン13件は、設定変更後に新規起動したセッションでも全件未到達だった。① 許可ドメインの保存先環境とスケジュールタスクの実行環境が同一かを確認する（最有力の原因） ② `.google` TLD の3件（`blog.google` / `deepmind.google` / `gemini.google`）が `*.google.com` で拾えないため個別指定になっているかを確認する ③ `www.aisi.gov.uk` / `www.axios.com` / `www.cnn.com` ＋ `openrouter.ai` を次回追加対象とする
- 判定手順: **まず WebFetch を試し、成功したら障害として記録しない**。失敗した場合、文面が `EGRESS_BLOCKED` ならゲートウェイ拒否で確定（`curl` 不要）。一般的な403文面なら `curl -sS -o /dev/null -w "%{http_code}" --max-time 25 <URL>` で切り分ける
- ⚠️ **WebFetch の要約取りこぼしには2つの形がある**（B-024）
  - **最上位が落ちる形**: changelog / release notes は「最上部から日付つきで3件以上列挙せよ」と問う。08-09 も `code.claude.com` と `raw.githubusercontent.com` の2ソースで最上位一致を確認し v2.1.226 を確定
  - **同一日の2件目以降が落ちる形（08-09 に判明）**: `github.blog` は 8/7 に5件出ていたが 08-08 のセッションは2件しか取れていなかった。**同一日に複数出るソース（`github.blog` / `claude.com/blog`）は「`<前回チェック日>` 以降のエントリを全て挙げよ」の形で問うこと**
- ⚠️ **JST 早朝実行による構造的な検出遅れ**: UTC 前日昼以降の公開は翌日検出になる。取りこぼしとは区別する
- WebFetch / RSS 安定（一次取得）: `code.claude.com/docs/en/changelog`、`github.com/*/releases`（Copilot / Codex CLI / claude-code）、`github.blog/changelog/*`、`platform.claude.com/docs/*`、`raw.githubusercontent.com`、`blog.modelcontextprotocol.io`、`developer.apple.com`、`ai.google.dev`、`huggingface.co`（API・HTML）、`cursor.com/changelog/rss.xml`、`forum.cursor.com/c/announcements/11.rss`、`support.claude.com`、`claude.com/blog`、`community.openai.com/c/announcements/6.rss`、`developers.openai.com/changelog`、`registry.npmjs.org`、`pypi.org`

## 注目スケジュール

- **8/9**: ChatGPT Atlas シャットダウン
- **8/10**: 週次復旧チェック（月曜）／ Gemini in Classroom が全年齢の生徒へ開放（web）
- **8/12**: Made by Google ／ Gemini 3.5 Pro ローンチの噂（Google 未発表）
- **8/14**: Claude Code の既定権限モードが auto mode へ（Pro / Max / Team）
- **8/17**: Claude Console 旧 Workbench 退役 ＋ 実験的プロンプトツール API 廃止 ／ Gemini API の Imagen 4.0 系3本停止 ／ Gemini in Classroom のモバイル開放
- **8/19**: Claude Code 週次上限50%増の終了
- **8/20**: Gemini Enterprise Agent Platform の Grok 4.1 ファミリー停止（一次未確認）
- **8/26**: OpenAI Assistants API 廃止 / o3 退役 / GPT-4.5 完全廃止 ／ Copilot 既定モデル有効化ポリシー発効
- **8/30**: 公式 DALL·E GPT の退役
- **8/31**: GitHub Spark の既存ユーザーアクセス終了（エクスポート期限） ／ Sonnet 5 促進価格終了（→ $3/$15） ／ `gemini-robotics-er-1.6-preview` 停止 ／ GPT-5.4・5.4 mini が Codex（ChatGPT サインイン）から除外
- **9/1**: GitHub Copilot の全体験でモデル廃止
- **9 月**: iOS 27 / macOS 27 GA ／ App Store の新規申請・更新で Social Media 年齢レーティング回答が必須化 ／ auto mode の既定化を Enterprise・API・各クラウドへ拡大予定
- **12/2**: EU AI Act の生成コンテンツ標識義務、8/2 以前に市場投入済みシステムへの猶予終了
